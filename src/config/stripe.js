// Stripe configuration
export const STRIPE_KEY = 'YOUR_STRIPE_PUBLISHABLE_KEY'; // TODO: Replace with your Stripe key

export const stripeConfig = {
  merchantDisplayName: 'ParkSmart',
  publishableKey: STRIPE_KEY,
};
